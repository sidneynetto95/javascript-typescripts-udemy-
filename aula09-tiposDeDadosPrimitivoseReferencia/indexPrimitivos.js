//tipos de Dados primitivos:
//Strings
//Number 
//Undefined 
//null (symbol, bigint)
//boolean.
// São imutaveis
// São passados por cópias



const nome = 'Sidney' ; //string
const nome1 = "Sidney" ; //string
const nome2 = `Sidney` ; //string
const num1 = 10; // number
const num2 = 10.52 // number
let nomeAluno; //undefined -> não aponta para local nenhum na memória
const sobrenomeAluno= null; // Nulo -> não aponta para locar nenhum na memória
const aprovado =  true; // Boolean = true, false(valores lógicos)
// comando typeof para ver qual o tipo da variável
console.log(typeof nome, typeof num1);


